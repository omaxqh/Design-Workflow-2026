# 2026-05-15

- 处理酒店列表页优惠条异型优惠标签时，`LIST.HOTEL_CARD.SPECIAL_PROMO_BADGE#HUNDRED_BILLION_SUBSIDY` 必须使用原字体 `FliggyFont Medium Italic`。字体包在 `/Users/tianzhongyi/Library/Fonts/FliggyFonts-MediumItalic.ttf`，macOS 字体册识别为 family `飞猪旅行体`、style `Medium Italic`。如果 Figma MCP 无法 `loadFontAsync({ family: "FliggyFont", style: "Medium Italic" })`，必须暂停报告，不能自动降级为 `Fliggy Sans 102` 或其他字体。
- 处理新增模块 / 新增页面时，确认后的 image gen 方案图是 Figma 还原的视觉标准，不是灵感参考。写入 Figma 前和写入过程中必须对照方案图视觉锚点；交付前必须保存方案图与 Figma 设计稿的并排截图对比。方案图中的轻拟物 icon、皇冠、券卡、插画、光效、质感物件等装饰元素必须切图/生成透明 PNG 后上传 Figma，禁止用基础可编辑图层重画替代。
