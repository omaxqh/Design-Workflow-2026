# SmartDesigner — 智能设计决策助手

> 理解产品需求，结合设计原则，输出可直接驱动设计执行的 DRD（Design Requirements Document）。

---

## 触发条件

当用户提供产品需求文档（PRD）、设计稿截图、或业务背景描述，并希望获得设计方向建议时使用。

触发词：`/smart-design`、"帮我分析设计方向"、"审阅这个方案"、"设计决策"

---

## 项目结构

```
SmartDesigner/
├── Step1_放入资料/          ← 按项目文件夹放入资料
│   ├── 示例Project/
│   │   ├── Prd/          # 产品需求文档
│   │   ├── 沟通纪要/     # 会议纪要、评审记录
│   │   └── 用研资料/     # 用户研究报告、调研数据
│   └── INPUT.md
├── Step2_用SKILL分析/      ← 本文件所在，定义分析流程
│   └── SKILL.md
├── Step3_查看分析结果/     ← 按项目名新建文件夹，输出 .md 文档
│   └── 示例Project结果/
├── DesignPrinciple.md      ← 设计原则知识库（通用原则/反模式/评分）
├── BussinessExp.md         ← 业务知识库（行业经验/业务规则/项目决策）
└── Reference               ← 参考方案
```

支持的输入类型：
- `.md` 文档（PRD、需求描述、会议纪要、用研报告）
- `.png` / `.jpg` 图片（设计稿截图、竞品截图）
- `.txt` / `.pdf` 文本（沟通记录、数据报告）
- *资料类型可拓展*

---

## 全局处理原则（跨 Step 生效）

### P0 · 凡设计师拍板必须触发 IDE 选项 UI

任何需要设计师做选择的环节（Step 1.E 方向级拍板、Step 2 方案级拍板等），**必须调用 `ask_user_question` 将选项渲染为 IDE（Qoder/Codex/Cursor）选项 UI**。禁止「只打印文本让设计师打字回复」。

**构造规范**：
- 一次 `ask_user_question` 最多 4 个问题，每个问题 2-4 个选项。大于 4 项分批提问
- **推荐项放首位**，label 末尾加 `（Recommended）`
- 禁止「Y/N 确认型」问题，确认态内置在具体方案选项中
- 禁止「只给一个选项」，每个关键决策点至少 2 个方案

#### 🔒 P0 子规则 · IDE 问答「人话 + 双字段双备份」硬制

**核心原则**：设计师不看过程文档，只通过 IDE 气泡做选择。所有文字必须让**没读过本 SKILL / PRD / 之前对话的人**一眼看懂。

#### 📦 双字段双备份（最关键）

**事实**：IDE 渲染 `ask_user_question` 时，`label` 和 `description` 任一可能被单独显示（不同版本/折叠态不一致）。因此两个字段必须**各自自包含**。

**硬制规则**：`label` 和 `description` **内容完全一致** · 都是同一句完整表达，包含四要素：

1. **【动作词】起头**（写/放/显示/收起/接入/换成/加上……）
2. **方案成什么样**（用户会看到什么文字/图形/操作）
3. **好处**（用用户/业务语言，如「一眼看到省多少」「想找一步到位」）
4. **代价**（以「代价：」起头，接约束/兜底）

**推荐句式**：`【动作词】……方案……。好处：……。代价：……。`

**✅ 示例**（两字段完全一致）：
```
label：在快筛栏首位放一个金色「只看 88 房」按钮，点一下列表只留专享房。好处：会员想找专属房一步到位。代价：点开后可选酒店会减少。（Recommended）

description：在快筛栏首位放一个金色「只看 88 房」按钮，点一下列表只留专享房。好处：会员想找专属房一步到位。代价：点开后可选酒店会减少。
```

**❌ 禁止分工写法**：禁止「label 只写方案 + description 只写代价」；禁止「label 写长句 + description 写补充」。宁愿重复书写，不能分工。

#### 🚨 四条硬闸（累积反馈沉淀 · 每一条都曾翻车）

**硬闸 1 · 动作词起头**
- 每个选项必须以**动作词**起头（写/放/显示/收起/接入/换成……）
- ❌ 禁止以「代价」「前提」「结果」「违背 XX 共识」等评价/后果起头
- 例：❌「商家数据不齐时接住」「违背方向共识」 → ✅「有普通价就写对比、没有就回单句」

**硬闸 2 · question 禁 Y/N**
- question 必须以「怎么」「如何」「写什么」「显示什么」「放什么」起头
- ❌ 禁止「要不要」「做不做」「加不加」
- 原因：Y/N 型问题会逼选项退化成「加了但…」「不加但…」，全是代价叙述
- 确认态内置在 A/B/C 里，C 可以是「本期彻底不做 X」，但仍以动作词起头（如「把 X 模块下掉」）

**硬闸 3 · 选项主张对立**
- A/B/C 必须是三种不同的**主张/态度**，不是同一方案的细节差异
- 每个选项的「好处」必须互斥：A 的好处是 B 没有的
- ❌ 翻车示例：A「三处都写对比句」B「详情页写短版、其他页写长版」——首句都在讲「在哪写什么」，像同一句话被拆开
- ✅ 修正：每条加「立场词」首句，如「全场押对比 / 详情页留空间 / 按数据随变」

**硬闸 4 · 不问方法论**
- Agent 只问设计师「产品该啥样」，**不得**问「这条规则是否合理」「内容能否看懂」「方法论过不过关」
- 设计师的时间只用于拍板产品方向，不是验证 Agent 的工作质量
- 自检必须在 Agent 心里过，不能当问题丢出去

#### ⛔️ 禁用术语黑名单（question / label / description 全部不得出现）

| 类别 | 禁用词 | 替代表达 |
|---|---|---|
| 内部编号 | `张力 1/2/3`、`Q1-Q6`、`1.E`、`2.B`、`gap 1/2/3`、`设计点 N`、`决策 N` | 直接重述业务含义，如「列表标签要不要写金额」|
| 设计原则缩写 | `DP 一.3`、`BE 二.V10`、`DP/BE 引用`、`D1-D6` | 翻译成一句话好处，如「让用户一眼看到省多少」|
| 画像代号 | `画像 A/B/C`、`Richard / Jessica / 张先生` | 直接说人群特征，如「追效率的商旅用户」|
| 设计方法论黑话 | `流程优化`、`信息重构`、`认知负荷`、`转化漏斗`、`链路连贯`、`心智`、`锚点`、`差异感知`、`庆祝感`、`既要又要` | 用最朴素的用户动作/感受，如「用户点得到能用」「省了多少心里有数」|
| 量化代号 | `P0/P1/P2`、`IPVUV/D2O`、`≥120px`、`44°`、`#E8B855` | 必须提视觉时只说「金色 · 斜切异形」，不放具体数值 |

#### 🧪 发出前自检五问（任一答「否」必须重写）

1. **双字段一致**：`label` 和 `description` 内容完全相同？单看任一都能选？
2. **动作词起头**：每个选项是否以动作词起头？（禁评价/后果起头）
3. **question 非 Y/N**：是否以「怎么/如何/写什么/显示什么/放什么」起头？
4. **选项对立**：A/B/C 是三种不同主张？每条好处互斥？
5. **禁用词清零**：文本中有没有张力/DP/BE/画像/心智 等黑名单术语？

拿手指遮住气泡模拟「只看单边能不能选」，能选再发。

#### 📏 字数建议

- `question` ≤ 40 字，直接点明「在哪个页面 · 要决定什么」，以「怎么/如何/写什么」起头
- `label` 与 `description` 内容一致 · 各 ≤ 80 字 · 装下「动作词起头 + 方案 + 好处：… + 代价：…」四要素

### P1 · 凡方案推荐必须引用 DP/BE 具体条款（仅限过程文档）

任何「推荐/不推荐/优势/劣势」结论必须附上 `DP 一.X` 或 `BE 二.V10` 类的具体条款引用，禁止凭感觉下结论。

⚠️ **适用范围**：P1 仅适用于**过程文档**（`01.E_PRD健康度_拍板结果.md`、`02_设计点聚合.md`、DRD 中的「原因」「依据」列等，Agent 自用的推理记录）。

⛔️ **禁止延伸到 IDE 选项 UI**：`ask_user_question` 的 question / label / description **一律按 P0 子规则写人话**，把原则翻译成一句好处，不得出现 DP/BE 缩写。

### P2 · 设计师分两次拍板 · 方向先行 · 方案收尾

- **第一次拍板 · Step 1.E**：**方向级**。只针对「PRD 内部『既要又要』张力」与「建议质疑清单」，选 `偏 A / 偏 B / A-B 分场景共存 / 按建议改 / 维持原案 / 延后本期 / 放弃`。不涉及具体方案 UI。
- **第二次拍板 · Step 2.C**：**方案级**。针对每个设计点的 2-3 个候选方案，挑具体落地。

两次拍板职责不同不重复。第一次拿「方向共识」再做第二次「方案落地」——避免让设计师在「方案细节」和「PRD 本身对错」两层一起判断。

**两次拍板均必须调用 `ask_user_question` 触发 IDE 选项 UI**，不得让设计师打字回复拍板。其他阶段（1.D 6 要素方向、5.5 分层、6.0 字典冲突等）仍按对应章节规则。

---

## Pipeline（分步执行）

### Step 1：需求 & 用户洞察 · 人话版结论（合并 PRD 读取 + 用户研究 + gap 分析）

**目标**：一步到位产出给设计师看的「6 要素人话版结论」，避免设计师在一堆表格里翻找。

> **设计原则**：设计师要的不是结构化表格，是**能直接讨论的结论**。所以这一步把「PRD 原文提取 + 用户画像 + 初步 gap + 设计思路方向」整合成一张表，以人话呈现，设计师看完就能给方向反馈。

#### 1.A 输入来源（按优先级）

1. 用户直接提供的 PRD 链接（如 alidocs.dingtalk.com、yuque.antfin.com 等）→ 使用 **IDE 内置 Browser Agent** 打开并提取文档内容
2. 本地文件：读取 `Step1_放入资料/[项目名]/` 中所有子文件夹的资料
3. 用户口述的需求描述

> **PRD 链接处理（默认路径，不得绕过）**：
>
> **Step A · 默认**：调用 Qoder/IDE 内置 **Browser Agent**（subagent_type=Browser）打开链接。IDE 浏览器共享用户 SSO 登录态，内网文档（aliyuque.antfin.com / alidocs.dingtalk.com / yuque.antfin.com）大多数情况可直接读取。
>
> **Step B · 遇登录拦截时**（页面空壳 / 跳 SSO / 401）：Browser Agent 在 IDE 浏览器窗口中**暂停等待用户现场完成登录**，登录完成后 Browser Agent 再次读 DOM 抽全文。全程无需用户导出 cookie 或复制粘贴。
>
> **Step C · 仅当 Browser Agent 整体不可用 / 登录持续失败时** → 中断并让用户二选一：
> - C1. `/setup-browser-cookies` 导入 aliyuque/antfin cookie 后重试（适合需要反复抓取的场景）
> - C2. 用户手动粘贴 PRD 全文（最后兜底）
>
> **禁止行为**：
> - 不得一上来就用无登录态的外部 fetcher（fetch_content 对内网返回 `loading` 即为无登录态）尝试后立刻升级到 C1/C2
> - 不得静默降级；从 A 到 B 到 C 任何一级切换都必须显式告知用户原因
> - 不得跳过 A 直接执行 C（参见：内网知识库访问失败交互规范）

#### 1.B 内部工作（不向设计师展示，沉淀为文件）

从获取的资料中提取以下信息（沉淀到 `Step3_查看分析结果/[项目名]/PRD原文.md`）：

```
- 行业 & 业务
- 业务背景
- 目标用户
- 用户痛点（按频次/强度排序）
- 产品方案（按功能维度列清单）
- 期望目标（定性 + 定量）
```

#### 1.C 用户画像生成（沉淀到 `用户画像报告.md`）

> 🔒 **硬闸：不得跳过**
>
> 1. 任何情况下都 **禁止**凭 PRD自行推导用户画像代替 1.C；凭空推导的结果不能写进 1.D 第 4 节
> 2. 未产出 `用户画像报告.md` → **禁止进 1.D 第 4 节**（会被 Step 1 末尾硬闸打回）
> 3. 访问/点击失败时，**必须先穷尽下方定位策略 S1 → S2 → S3 三次重试**，三策略都失败再走授权流程（见下方第 2 条）
> 4. 即使用户有替代资料（用研报告/组内沉淀画像），也必须明示告知用户「将跳过在线工具、改用 XX 资料作为画像来源」并等待确认后才计作通过

**执行方式：通过「用户画像生成器」在线工具生成**

🔒 **调用 Browser Agent 的硬闸**：调用 sub-agent 执行本节时，**prompt 必须原样引用本节下面 「执行顺序 1…5」 的全部原文**（包含 a/b/c/d/e 五个子步骤 + S1/S2/S3 三定位策略 + 点击前验证 + 6 条反例），不得改写、不得摘引、不得只传「大意」。主 agent 唯一要做的预处理是：把 1.C 中「填写 4 个表单字段」的 4 变量占位符替换为本项目从 Step 1.A 提取的具体值（行业/用户/痛点/关键词）。

❌ 已发生过的错误：主 agent 自作主张写「在聊天框输入提问：飞猪酒店会员…」，跳过了 b 填字段、c 输「开始分析」两步 → 工具拿不到结构化变量 → sub-agent 重复试错卡死。

工具地址：`https://aistudio.alibaba-inc.com/aiWorktable#/aistudio/webPage?appCode=zgewbVfNDRr`

执行顺序：
1. 使用浏览器打开上述工具页面（Qoder 内置浏览器已有登录态可直接使用）
2. 如访问失败（未登录/cookie 过期/页面无响应），**必须调用 `ask_user_question` 让用户显式拍板**（禁止自作主张选 A/B/C）：
   ```
   question: 用户画像生成器访问失败（原因：[具体错误]），怎么继续？
   options:
     A. 执行 `/setup-browser-cookies` 导入内网登录态后重试（Recommended）
     B. 跳过在线工具，改用用户提供的用研资料生成画像（需指定文件路径）
     C. 跳过在线工具，凭 PRD 信息独立推导（需用户明示允许降级）
   ```
   - 未收到用户拍板前，禁止继续 1.D
   - 选 B/C 均需在《用户画像报告.md》顶部标注「来源：用研资料 / PRD 推导」并由用户确认
3. 访问成功后，按以下步骤操作页面：
   
   **a. 展开变量面板**：点击页面底部的「展开变量」按钮（如已显示「收起变量」则已展开）
   
   **b. 填写4个表单字段**：
   - **行业分类** ← Step 1「行业 & 业务」
   - **用户是谁** ← Step 1「目标用户」
   - **用户遇到了什么问题** ← Step 1「用户痛点」
   - **知识库索引关键词** ← 从 Step 1 提取 3-5 个核心关键词
   
   **c. 发送消息**：在底部消息输入框（placeholder「请输入」）中输入 `开始分析`
   
   **d. 点击发送**：页面底部工具栏有 3-5 个同类 `.chat-input-icon-btn`（上传/语音/发送都是同 class），**必须精确锁定发送键**。

   🎯 **核心原则 3 条（任缺一条都会失败）**：

   1. **点击目标永远是 `div.chat-input-icon-btn` 父级**，不是 `<svg>` 或 `<use>` — click 事件监听器绑在 div 上，点子元素不保证触发业务逻辑
   2. **靠内部 `<use>` 的 `xlink:href="#icon-chat-run-btn"` 识别发送键** — 这是发送按钮的唯一稳定特征
   3. **CSS 选择器不能用 `[href=...]`**，因为真实 DOM 是 `xlink:href`（带 XML 命名空间），必须用 `[*|href=...]` 才能匹配到

   **DOM 真实形态**（来自 idealab 在线工具实际抓取）：
   ```html
   <div class="chat-input-icon-btn">  ← 👈 这里是点击目标
     <i class="next-icon next-medium">
       <svg class="next-icon-remote" style="font-size: 18px;">
         <use xlink:href="#icon-chat-run-btn"></use>  ← 👈 这里是识别特征
       </svg>
     </i>
   </div>
   ```

   **定位策略（按优先级，前面失败再降级）**：

   **S1 · CSS `:has()`（首选 · 90% 场景可用）**
   ```css
   /* 注意：必须 *|href 不能 href，必须选 div 不能选 use */
   div.chat-input-icon-btn:has(use[*|href="#icon-chat-run-btn"])
   ```

   **S2 · XPath（避开 CSS 命名空间坑 · 兼容性最好）**
   ```xpath
   //use[@*[local-name()="href" and .="#icon-chat-run-btn"]]/ancestor::div[contains(@class,"chat-input-icon-btn")][1]
   ```

   **S3 · JS 代码兜底（前两个都取不到时）**
   ```javascript
   Array.from(document.querySelectorAll('div.chat-input-icon-btn'))
     .find(d => d.querySelector('use')?.getAttribute('xlink:href') === '#icon-chat-run-btn'
              || d.querySelector('use')?.getAttribute('href') === '#icon-chat-run-btn')
   ```

   **点击前强制验证（不验证不得点击）**：
   - 锁定到元素后，先打印该元素的 `outerHTML`
   - 验证 outerHTML 里必须**同时包含** `chat-input-icon-btn` 和 `#icon-chat-run-btn`
   - 验证通过才点击；不通过立即回退到下一个策略
   - 点击后 3 秒内如果页面无任何变化（无 loading / 无新消息气泡 / 输入框未清空）→ 判定点击失败，尝试下一策略

   ❌ **禁止**的错误定位方式（血泪教训）：
   - ❌ 直接选 `use[*|href=...]` 作为点击目标 — click 监听在父 div 上，点子元素不触发
   - ❌ 用 `use[href="..."]` — xlink:href 带命名空间，CSS 普通 `[href]` 选不到
   - ❌ 用「▶ 小飞机图标」这种视觉描述 — Browser Agent 会扫遍整页
   - ❌ `.chat-input-icon-btn:last-child` / `:nth-child(N)` — DOM 顺序会变（上传/语音/发送顺序非稳定）
   - ❌ `data-aplus-clk="x9_xxx"` / `data-spm-anchor-id` — 埋点 ID 每次刷新都变
   - ❌ 不做点击前验证，盲点第一个匹配到的元素
   
   **e. 等待生成**：等待 30-60 秒，AI 会返回完整的用户画像报告
   
4. 提取生成的用户画像内容（包含：典型画像、用户原语、情景描述、报告出处）
5. 将生成结果整合到本步骤输出中

> **注意**：报告生成需要时间，不要重复点击「展开变量」或重复发送消息，耐心等待结果完整输出。

**用户画像内部产物**（沉淀到 `用户画像报告.md`，**不作为设计师主要交付物**）：

1. 2-3 个典型用户画像（年龄/行为特征/核心诉求/用户原语）
2. 每个用户在当前场景下的具体使用情景
3. 认知习惯和决策偏好
4. 最敏感的体验触点

---

#### 1.D 最终交付物：6 要素人话版结论（🎯 设计师只看这一份）

**格式硬性要求**（全部用人话，禁止堆表格堆名词）：

```markdown
## 1. 项目背景
[一段话 · 3-5 句] 这是什么业务，当前处于什么状态，为什么要做这件事。

## 2. 核心问题（产品想解决什么）
- 问题 1：[具体场景化描述，用户真实会遇到]
- 问题 2：...
- 问题 3：...
（最多 3 条，不能是抽象词如「体验差」「感知弱」）

## 3. 产品方案（PRD 提出怎么解）
- F1 · [功能名]：[一句话说清干什么]
- F2 · ...
（按功能维度列清单，每条一行，不展开细节）

## 4. 用户特征（我们的用户长什么样）
- 画像 A · [标签名]：[决策模式] + [最敏感触点]
- 画像 B · ...
（2-3 个画像摘要，每个 2-3 行）

🔒 **第 4 节所有画像必须来自 1.C 产出的 `用户画像报告.md`**（需节选引用关键原语 / 触点）；禁止凭 PRD 自行构造画像。若 1.C 走了 B/C 降级路径，本节需明标「来源：用研资料 / PRD 推导（已用户授权）」。

## 5. 方案 vs 用户 · gap 分析

| # | 产品方案 | 用户真实心智 | gap 是什么 |
|---|---------|------------|-----------|
| 1 | [方案点] | [用户会怎么想/怎么用] | [落差的具体描述] |

**关键问题**：对每个方案问两件事——
- 这个用户**会不会用**？（触发成本/理解门槛）
- 用了**有没有效**？（能不能解决用户的真实问题）

## 6. 初步设计方向（方向级 · 不拍板）

> **此步只给方向性思考，不给具体方案、不要求设计师选。具体方案 + 拍板统一留到 Step 2。**

针对第 5 部分 gap 表里每条 gap，给 1-2 句话的方向性思考：

```markdown
- gap 1 → 方向：[一句话的想法，如「权益金额不务求精确，用预估锚点替代」]
- gap 2 → 方向：...
- gap 3 → 方向：...
```

**本步的价值**：
- 先对齐方向（设计师是否理解对 PRD 意图），避免在 Step 2 给完一堆方案后才发现方向偏了
- 设计师可以在这一步修正方向（「这个 gap 我觉得不用改，用户实际没这需求」）
- 方向级描述即可，不要落到按钮颜色/文案字数等执行细节，那些是 Step 2 的事
```

**硬闸**：
- 6 项全部输出，缺一不得进 1.E
- **1.C 未产出 `用户画像报告.md` → 禁止进 1.D 第 4 节**（凭空推导画像 = 跳步，打回重做）
- 1.D 第 4 节画像来源必须明标（在线工具/用研资料/PRD推导三选一）；后两者需用户显式授权
- 用人话，不用「差异感知」「认知负荷」这种设计黑话（除非设计师主动要）
- gap 表至少 3 行，少于 3 行 → 说明 PRD 几乎无问题，需要主动质疑「是不是分析不够深」
- **1.D 6 要素方向不调用 `ask_user_question`**（打字回复即可修正方向）；**方向级拍板统一放在 1.E**（强制触发 IDE 选项 UI）

---

#### 1.E · PRD 自身健康度审查（🎯 第二份交付 · 方向级拍板 · 强制触发 IDE UI）

> **目的**：在 1.D 之后、Step 2 之前，把 PRD 自身「既要又要」类张力和「不合理需求」两类问题暴露出来，让设计师**方向级拍板**。
>
> **为什么要做**：如果 PRD 自身「既要低价又要尊贵」「既要简洁又要透明」「既要自动享受又要手动筛选」……这类自打架诉求没先对齐方向，Step 2 方案会在不同张力间反复摆荡；等到方案层才拍板 = 让设计师同时判断「方案细节」+「PRD 本身对错」两层，认知负担翻倍。
>
> **不做**：此步不给具体方案、不决定 UI 细节。只拍「两极方向 / 分场景共存」或「按建议改 / 维持 / 延后 / 放弃」。

##### 1.E.1 · 「既要又要」识别器（通用判断标准 · 不再依赖固定清单）

**定义**：「既要又要」= PRD 里**同时**出现两个**在同一用户路径上相互抵消**的诉求，且没有显式优先级。

**识别三问**（命中任一问回答"是"→ 判定为张力 · 必须进 1.E.3 拍板）：

1. 📏 **测量 A 的成功 = 测量 B 的失败吗？**
   例：F1 首页权益组件"曝光全权益"（A 成功）必然压缩"首屏房型曝光"（B 失败）。两者争同一屏幕面积/用户注意力/跳出率。
2. 🎭 **同一用户在同一触点上要两套心智吗？**
   例：让 88VIP 既感受"省钱实惠"又感受"尊贵特权"——情绪属性相反，同一触点同时给会糊。
3. 🎯 **两条 PRD 需求落到代码/设计时必须二选一吗？**
   例：F3 合并 icon 做「汇总」还是「逐项透明」——页面空间决定只能选一个。

**张力对参考库**（不限于此 · 每次扫描按本 PRD 自由扩展，凡命中上面三问均需列入）：

| # | 张力对 A | 张力对 B | 典型触点 |
|---|---|---|---|
| 1 | 低价 / 打折 / 性价比 | 尊贵 / 高端 / 身份感 | 会员入口、专享价文案 |
| 2 | 简洁汇总 / 精简 | 全展示 / 逐项透明 | 优惠明细、权益清单 |
| 3 | 高亮身份（88VIP/金卡）| 去身份化 / 无排斥 | 针对会员的文案替换 |
| 4 | 引导决策 / 紧迫限时 | 降心智负荷 / 庆祝感 | 倒计时、促销氛围 |
| 5 | 权益全曝光 | 首屏不转移购买决策 | 首页组件、列表插入块 |
| 6 | 专享价明晰 | 保护商家毛利 / 模糊价 | 下单算价、注册红包 |
| 7 | 自动享受 / 免手动 | 可筛选 / 可控制 | 会员筛选项、默认排序 |
| 8 | 个性化推荐 | 运营可配置 / 流量可控 | 权益组件、推荐 slot |
| 9 | 标识差异化（会员 vs 非会员）| 不伤害非会员心智 | 文案替换、标签密度 |
| 10 | 拓展性强 / 标准化 | 实现简单 / 本期快上 | 数据识别、工具配置 |
| 11 | 确定性承诺（退改保障）| 商家供给约束 / 成本控制 | 售后权益、保障文案 |
| 12 | 统一口径 / 一端为主 | 多端差异适配 | 跨端文案、Token 同步 |
| … | *命中三问定义就列入，不限于此表* | | |

**扫描产物（必输出 · 即使全部不命中也要明文写出）**：

```markdown
### 1.E.1 · PRD 「既要又要」命中扫描

| # | 张力对 A ↔ B | 命中等级 | 冲突证据（PRD 条款引用）| 不拍板会怎样 |
|---|---|---|---|---|
| 1 | [A] ↔ [B] | 🔴 强 / 🟡 中 / 🟢 弱 / ✅ 不命中 | PRD 第 X 条 + 第 Y 条 | [Step 2 会怎么摆荡] |
```

- 🔴/🟡/🟢 命中 → 必须进 1.E.3 拍板
- ✅ 不命中 → 显式列出并说明理由（"全部不命中"需主动质疑"是否扫描不深"）

##### 1.E.2 · 建议质疑的 PRD 需求清单

逐条对 PRD 条款打标，命中任一即列入：

- 违反 DP 反模式（虚假紧迫 P1 / 信息过载 P2 / 暗黑模式 P0 / 决策瘫痪 P2 / 认知断裂 P1 / 压力文案 P2 / 模糊状态 P1）
- 违背 BE 行业经验（如 BE 一.会员场景「自动享受 > 手动领取」、BE 二.V10「差异感知+庆祝感」）
- 用户画像明显用不上（画像误解 / 画像用不到 / 画像不感兴趣）
- PRD 条款与业务目标底层矛盾

**产物格式**：

```markdown
### 1.E.2 · 建议质疑清单

| # | PRD 条款原文摘要 | 违反什么（DP/BE/画像/目标）| 建议处理 |
|---|---|---|---|
| 1 | [原文摘要] | DP 一.3 / BE 一.会员 / 画像 A 用不上 | [改文案 / 降级 / 延后一期 / 放弃] |
```

无任何命中 → 明文写「PRD 需求全部合理」+ 主动质疑「是否审查不够深」。

##### 1.E.3 · 触发 IDE 选项 UI · 方向级拍板（🔒 强制硬闸）

把 1.E.1 🔴/🟡/🟢 命中 + 1.E.2 所有条目汇总，**必须调用 `ask_user_question`** 让设计师方向级拍板。

**调用规范**：
- 一次 `ask_user_question` 最多 4 个问题；超 4 条分批（本批拍完再发下一批）
- 每个问题 2-4 个选项；**推荐项放首位 · label 末尾加 `（Recommended）`**
- 「既要又要」条目 → 3 选项表达具体分歧（使用人话标签，禁止「偏 A / 偏 B / A-B 共存」抄字母）
- 「建议质疑」条目 → 2-3 选项：按建议改的人话写法 / 保持原样 / 本期不做
- **严格遵守 P0 子规则「人话三要素」**：每条 label 要有「改哪里 + 方案 + 好处」，禁用黑名单术语
- **禁止 Y/N 型**（"要不要处理这个矛盾"）；确认态内置在具体选项里
- **禁止只给一个选项**；每个张力至少 2 方向
- 设计师选 Other → Agent 必须复述理解再追问一次确认

**示例**（人话三要素 · 禁用术语/编号）：

```json
{
  "questions": [{
    "question": "列表页房型卡片上的 88VIP 标签，要不要把「具体省多少钱」直接写出来？",
    "header": "88 标签写法",
    "options": [
      { "label": "列表卡片内的 88 标签写「88VIP 立省 30 元」具体数字。好处：不用点开就能一眼看到省多少（Recommended）", "description": "标签吃一行空间，靠上滑收起弥补" },
      { "label": "列表卡片内的 88 标签写「88VIP 专享」四字，具体金额点开后浮层看。好处：标签更紧凑不占地方", "description": "用户不点开看不到金额，没看就划走" },
      { "label": "便宜商品写具体金额，高端酒店只写「88VIP 专享」。好处：两头兼顾", "description": "商品要额外打标，做法复杂" }
    ]
  }]
}
```

##### 1.E 硬闸

- 1.E.1 张力命中表必出（全不命中也要写「全不命中 + 理由」）
- 1.E.2 质疑清单必出（全合理也要写「全合理 + 主动质疑深度」）
- 1.E.1 🔴/🟡/🟢 命中条目 + 1.E.2 所有条目 → 必须全部进 1.E.3 `ask_user_question`
- **未触发 IDE 选项 UI → 禁止进 Step 2**
- 1.E.3 拍板结果必须沉淀到 `Step3_查看分析结果/[项目名]/01.E_PRD健康度_拍板结果.md`，供 Step 2/4 引用
- 允许设计师"全部接受推荐"一次性回应；但未完全回复即默认不得推进

---

### Step 2：设计点聚合 + 方案级拍板（继承 1.E 方向共识 + D1-D6 匹配）

**目标**：把 Step 1 识别出的 gap，加上 PRD 逐条审核找到的遗漏损伤点，全部聚合为「设计点」，每个设计点给 2-3 个候选方案（人话描述），一次性触发 IDE 选项 UI 让设计师拍板完成。

> **核心理念**：不让设计师分两轮拍板。PRD 审核 + 机会点补全 + D1-D6 匹配 都放在 Step 2 内部推理，对外只暴露「设计点 × 候选方案」一份让设计师选。

**输入**：
- Step 1 的 6 要素结论（背景/问题/方案/用户/gap/初步方向）
- `../DesignPrinciple.md`（通用设计原则 + 反模式清单）
- `../BussinessExp.md`（行业经验 + 业务规则）

#### 2.A 内部工作（不对外展示 · 不给设计师看表格）

以下六项都是 Agent 内部推理，目的是保证 2.B 的设计点清单完整。禁止输出审核表丢给设计师看（这会变成另一次拍板）。**但前 3 项的结论必须浓缩成 2.B.0「⚠️ PRD 审核警报」对外报告（见下文）**。

1. **PRD 逐条审核 · 体验四维**：每条 PRD 需求在「链路连贯性 / 信息完整性 / 文案可读性 / 交互好用性」四维度上是否有损伤点

2. **➕ 业务目标匹配度审核**（新 · 防「解了体验但没达成业务目标」）：
   - 逐条抽取 PRD 开头的「期望目标 / 业务目标 / 核心指标」
   - 反查产品方案清单，逐项判定「能达成 / 部分达成 / 无法达成 / 缺方案」
   - 出现「无法达成 / 缺方案」→ 必须在 2.B 补新设计点去补成（防止只解体验损伤、丢业务目标）

3. **复用 Step 1.E.1 · PRD「既要又要」拍板结果**（不再重复扫描 · 防两轮方向拍板）：
   - 读取 `01.E_PRD健康度_拍板结果.md` 的张力命中表 + 设计师方向选择
   - 已拍板「偏 A / 偏 B / A-B 共存」的张力 → 在 2.B.1 按该方向生成候选方案
   - Step 2 阶段若新发现张力 → 退回 1.E.1 补扫并补拍；禁止在 Step 2 重开方向级拍板

4. **复用 Step 1.E.2 · 建议质疑清单拍板结果**（不再重复扫描）：
   - 读取质疑清单 + 设计师选择（按建议改 / 维持 / 延后）
   - 「按建议改」→ 在 2.B.1 变成设计点进入方案级拍板
   - 「维持 PRD 原案」→ 记入 2.B.0 警报表「已质疑但维持」列，Step 2 阶段设计师再次确认不改
   - 「延后本期不做」→ 2.B.0 警报表 + Step 4 风险提示双留记录

5. **三视角审视**：用户视角（画像怎么用） / 原则视角（DP 什么反模式） / 业务视角（BE 行业经验是否违背）

6. **D1-D6 匹配**：每个损伤点匹配「设计思维深度」（D1 问题重构 → D6 文案修正），决定方案的干预强度。内化使用，不打标签给设计师看

   D1-D6 简映射：
   - D1 问题重构：PRD 问题定义本身偏了（罕见）
   - D2 方案替代：问题对但有更好的解法（新页可被原地变态替代等）
   - D3 流程优化：链路有多余步骤 / 触达时机不对
   - D4 信息重构：内容对但层级/优先级不对
   - D5 组件调整：结构对但某个组件视觉/交互要改
   - D6 文案修正：都对，只是措词不对

   匹配规则：**默认从 D6 往 D1 查，优先匹配最浅层**。这个匹配会自然反映在 2.B 的方案里。浅层不够时升级，升级理由写在推荐语中

**三个升级信号**（用于在方案中体现，不用明说出 D 维）：
- D6 → D5：改完文案还是看不懂 / 文案没找到放的地方
- D5 → D4：组件调完仍信息过载 / 该重要的还是不重要
- D4 → D3：信息层级调好了但用户完成任务路径仍断裂

#### 2.B 对外交付：设计点 × 候选方案（🎯 设计师只看这一份）

##### 2.B.0 ⚠️ PRD 审核警报（强制输出 · 无警报也要写「三项均通过」）

把 2.A 前 3 项的结论浓缩成一张表展示给设计师（**设计师一眼就能看到哪里需要回怨 PM**）：

```markdown
## ⚠️ PRD 审核警报

### 1. 业务目标匹配度
| 业务目标 | 是否能达成 | 说明 |
|---|---|---|
| [PRD 开头提的目标 1] | ✅ 能 / 🟡 部分 / ❌ 不能 / ⚠️ 缺方案 | [一句话说为什么] |

> 出现 ❌ / ⚠️ → 已在下方设计点 [X] 中补方案
> 全部 ✅ → 写「产品方案能覆盖全部业务目标」

### 2. PRD 内部矛盾
| # | 矛盾点 | PRD 引用条款 | 处理建议 |
|---|---|---|---|
| 1 | [两条 PRD 诉求打架] | PRD 第 X 条 vs PRD 第 Y 条 | 作为设计点 [N] 为设计师拍板 |

> 无矛盾 → 写「PRD 内部无明显前后矛盾」

### 3. 建议质疑的 PRD 需求
| # | PRD 条款 | 问题 | 建议 |
|---|---|---|---|
| 1 | [原文摘要] | [违反 DP 三.X / BE Y / 画像用不上 / 自相矛盾] | [延后上线 / 改版 / 放弃 / 减规模] |

> 无不合理需求 → 写「PRD 需求均合理」
```

**硬制规则**：
- 三个表格每个都必须输出（无警报也要明文写「通过」），禁止省略
- 警报中的问题如影响方案选型，必须在下方对应设计点的「要解决什么」中提及（形成闭环）
- Step 4 产 DRD 时，警报结论需同步到「四·风险提示」或新设内部评审章节中留记录

##### 2.B.1 设计点 × 候选方案

**组合规则**：
- 设计点来源 = Step 1 的 gap（已对齐方向） **+** 2.A 发现的 PRD 遗漏损伤点 **+** 2.A 发现的业务目标缺方案点 **+** 2.A 发现的 PRD 矛盾点（作为「偏 A / 偏 B / 分场景共存」选择题暴露）
- 每个设计点 2-3 个候选方案，不是「推荐 + 兑数」
- 方案之间要有真实差异（做什么、做到什么程度、谁受益）

**每个候选方案的格式（说人话 · 不要成本列）**：

```markdown
### 设计点 [N] · [用人话设如点名，如「下单页权益怎么让用户看到」]
**要解决什么**：[用一句人话说清本设计点在解什么问题，不用术语]

🅰️ **方案 A · [说人话的方案名]**
- **怎么做**：[场景化描述 · 用户会看到什么 / 点什么 / 体验到什么]
- ✅ **好处**：[具体好处] · **依据**：`DP 一.X` 或 `BE 二.V10`
- ⚠️ **代价**：[具体代价 / 命中 `DP 四.X` 反模式（如有）]

🅱️ **方案 B · [...]**（同上结构）

🅾️ **方案 C · [...]**（如有）

👉 **推荐**：[A/B/C] · **理由**：[引用 DP/BE 具体条款]
```

**说人话硬制规则**：

| ❌ 反面示例 | ✅ 正面示例 |
|---|---|
| 「D3 流程·下单成功页+订详页双点触达」 | 「用户下完单立刻提示可以免费升房，订详页也保留入口，不让他关 App 后错过」 |
| 「B · 预估锚点+点击展开计算口径」 | 「标签写『预估价值 ¥XX』，用户点一下能看到这钱是怎么算出来的」 |
| 「D6 文案修正·状态+预期」 | 「Chat 第一句话就告诉用户『我已经帮你在跟酒店申请了，最晚 X 点给答复』」 |

**原则**：方案名和描述要让没看过本 SKILL 的设计师**一眼懂这是在做什么、做成啥样**。不出现 D1-D6 字眼，不出现「流程优化/信息重构/转化漏斗/认知负荷」等工程术语。

#### 2.C 触发 IDE 选项 UI（强制 · 一次拍完所有设计点）

打印完 2.B 文档后，**必须调用 `ask_user_question`** 让设计师在 IDE 里点选。

**调用规范**：
- 一次 `ask_user_question` 最多 4 个问题。设计点 > 4 时分批提问（第一批拍完再发第二批）
- 每个问题对应一个设计点，选项 2-4 个
- **推荐项放首位**，label 末尾加 `（Recommended）`
- **label 直接描述方案做什么、做成啥样**（不是缩略名）
- **description 补充场景细节和核心代价**（≤ 40 字 · 不写成本）
- 问题不用「Y/N 是否确认」型，确认态内化在选项里

**示例调用（人话版）**：

```json
{
  "questions": [{
    "question": "设计点 1 · 权益金额标签要写多精确？",
    "header": "权益金额",
    "options": [
      {
        "label": "写『预估价值 ¥XX』，点一下看怎么算出来的（Recommended）",
        "description": "保留价值锚点但不精确，规避算不准的合规风险"
      },
      {
        "label": "写『权益价值约 ¥XXX』精确金额",
        "description": "PRD 原方案，数字具体但算不准用户会来退单"
      },
      {
        "label": "写『预估 ¥150-300』区间表达",
        "description": "最保守，但数字弱化会削弱用户的价值感知"
      }
    ]
  }]
}
```

#### 2.D 硬闸

- 设计点至少 3 个，少于 3 个 → PRD 几乎无问题，需主动质疑「是不是 2.A 审核不够深」
- 每个设计点 ≥ 2 个方案，未引用 DP/BE 依据的方案标红重写
- label/description 出现 D1-D6 / 「流程优化」/「信息重构」 等术语 → 重写为人话
- D1/D2 主推占比 > 30% → 主动质疑「是否高估了问题深度」
- 未调用 `ask_user_question` 触发 IDE 选项 UI → 不得进 Step 4
- 设计师选了 Other → Agent 必须复述理解再追问一次确认

**内部 D1-D6 的作用**：仅作为 Agent 评估「设计思维深度」的内部依据，不对外曝露编号；对外文案一律按 P0 子规则写人话。

---

### Step 4：设计策略输出

**目标**：基于 Step 2 设计师一次性拍板结果，输出 DRD。只输出设计师选中的方案，不输出被放弃的方案。

基于前面的分析和选择，输出第一部分内容：

```markdown
# [项目名] — 设计需求文档（DRD）

## 一、核心策略
[1-2句话描述整体设计方向]
设计关键词：[3-5个关键词]

## 二、关键决策点
### 决策1：[选项A] vs [选项B]
- **推荐**：[选项X]
- **原因**：[关联用户痛点或设计原则]
- **具体方案**：[一句话描述落地方式]

### 决策2：...
### 决策3：...

## 三、文案规范
| 场景 | 推荐文案 | 避免文案 | 原则依据 |
|------|----------|----------|----------|
| ... | ... | ... | ... |

**文案原则**：
- [原则1]
- [原则2]

## 四、风险提示
| 风险 | 场景 | 规避方案 |
|------|------|----------|
| ... | ... | ... |

## 五、优先级排序
| 优先级 | 设计任务 | 理由 |
|--------|----------|------|
| P0 | ... | ... |
| P1 | ... | ... |
| P2 | ... | ... |
```

---

### Step 5：设计规格输出

**目标**：输出可直接驱动智能体或设计师执行的组件级规格。

基于 Step 4 的决策方向，逐页面/逐触点输出详细设计规格：

```markdown
## 六、设计规格

### 页面/触点 1：[页面名称]

**触发条件**：[什么情况下展示该模块]

**新增/改动元素**：
| 元素 | 位置 | 结构 | 内容规则 | 约束 |
|------|------|------|----------|------|
| [元素名] | [在页面中的位置] | [组成部分：图标+文字/卡片/标签等] | [内容填充逻辑] | [字数限制/数量限制/截断规则] |

**状态枚举**：
| 状态 | 触发条件 | 展示内容 | 交互行为 |
|------|----------|----------|----------|
| 默认态 | ... | ... | ... |
| 空态 | ... | ... | ... |
| 异常态 | ... | ... | ... |

**信息层级**（从高到低）：
1. [最重要的信息]
2. [次重要的信息]
3. [辅助信息]

**交互说明**：
- 点击行为：...
- 滑动/展开：...
- 动效要求：...

---

### 页面/触点 2：[页面名称]
（同上结构）

---

### 全局约束
- 权益展示最多 [N] 个，超出折叠
- 标签文案最多 [N] 字
- 信息密度：一屏不超过 [N] 个信息块
- 全链路一致性：[具体要求]
```

**输出位置**：保存到 `Step3_查看分析结果/[项目名]/设计需求文档.md`

> **项目资料沉淀规则**：每次 Pipeline 执行过程中获取的所有资料均按项目维度沉淀到 `Step3_查看分析结果/[项目名]/` 目录下：
> - `PRD原文.md` — 从链接/文件读取的 PRD 完整内容
> - `用户画像报告.md` — 用户画像生成器返回的完整报告
> - `01_需求洞察_6要素人话版.md` — Step 1.D 设计师唯一看的交付
> - `01.E_PRD健康度_拍板结果.md` — Step 1.E 方向级拍板结果（「既要又要」+ 建议质疑）
> - `设计需求文档.md` — Pipeline 最终输出的 DRD（终点）
> - 其他过程中获取的参考资料也一并存入

> ⚠️ **本 SKILL 的最终产物到此为止**：输出 `设计需求文档.md` 后整个 Pipeline 结束。后续的「设计范围分层 / HTML 稿生成 / 设计稿审阅」不属于本 SKILL 职责，由后续专门的 SKILL 承接。

---

## 执行规则

1. **不跳步**：必须按 Step 1→2→4→5 顺序执行，每步输出后等待用户确认再继续
   - Step 1.E 必须完成「既要又要」张力扫描 + 建议质疑清单，并强制触发 ask_user_question 方向级拍板；未拍完禁止进 Step 2
   - Step 2 继承 1.E 方向共识 + 内部 D1-D6 匹配（不对外展示），只向设计师交付「设计点 × 候选方案」并触发 ask_user_question 方案级拍板
   - Step 4 只输出被选中的方案，不输出被放弃的方案
   - Step 5 为本 SKILL 终点，输出 `设计需求文档.md` 后 Pipeline 结束
2. **可中断**：用户可以在任何步骤插入补充信息或纠正理解
3. **引用原则**：每个建议必须关联到 DesignPrinciple.md 中的具体原则
4. **不做美工**：输出信息架构和交互规格，不输出具体视觉方案（颜色/字号/间距像素值等）
5. **模块化**：如果用户已有前置产出（如已完成策略），可以跳过前置步骤直接进入对应 Step
6. **可执行**：Step 5 的输出必须达到「另一个智能体读完就能画出设计稿」的标准
7. **范围边界**：设计范围分层 / HTML 设计稿生成 / 设计稿审阅 等后续动作不属于本 SKILL，由后续专门 SKILL 承接

---

## 输出质量标准

### DRD 完整性检查清单

输出前必须自检以下条件全部满足：

- [ ] 每个决策点有明确推荐方向和理由
- [ ] 每个涉及的页面/触点都有独立的设计规格
- [ ] 每个新增元素有：位置、结构、内容规则、约束条件
- [ ] 每个元素有完整的状态枚举（默认/空/异常/加载）
- [ ] 文案有推荐和避免的对比表
- [ ] 全局约束条件明确（数量限制、字数限制、信息密度）
- [ ] 全链路信息一致性已标注
- [ ] 优先级排序有明确依据

### 质量红线

- 每条建议必须**具体、可执行**（不接受"优化用户体验"这种空话）
- 必须说明**为什么**（关联用户痛点或设计原则）
- 风险提示必须有**规避方案**
- 设计规格必须达到**另一个智能体读完能直接输出设计稿**的颗粒度

---

## 自进化机制

每次 Pipeline 执行完成并输出 DRD 后，执行以下自进化流程：

### 触发条件
用户对输出结果给出反馈（修改、补充、质疑、纠正）时触发。

### 执行步骤

1. **差异识别**：对比用户反馈与当前输出，提炼出 SKILL 本身缺失的信息类型：
   - 新的行业经验/业务规则？
   - 新的设计原则/反模式？
   - 流程步骤的缺失/顺序问题？
   - 输出模板结构不够完整？
   - 量化标准需要调整？

2. **提炼建议**：将差异归纳为具体的改进条目，格式如下：
   ```
   📝 本次执行发现以下可沉淀到 SKILL 的经验：
   
   1. [类型：原则/流程/模板/标准] — [具体内容]
      写入位置：[SKILL.md 的哪个章节 / DesignPrinciple.md 的哪个章节]
   
   2. ...
   
   是否将以上内容写入 SKILL 进行迭代？(Y/N/选择性写入)
   ```

3. **用户确认后写入**：
   - Y → 全部写入对应文件
   - N → 不写入，仅本次生效
   - 选择性 → 用户指定写入哪几条

4. **写入目标**：
   - 行业经验/业务规则/项目决策 → `BussinessExp.md` 对应章节
   - 设计原则/反模式/评分标准 → `DesignPrinciple.md` 对应章节
   - 流程调整/模板扩展/执行规则 → 本文件 `SKILL.md` 对应章节
   - 项目特有规则 → `Step1_放入资料/[项目名]/项目规则.md`（新建）

### 不写入的情况
- 纯粹个人偏好（非通用经验）
- 与现有原则矛盾且用户未明确表态替代
- 一次性的临时决策

---

## 示例用法

```
用户：帮我分析 示例Project 的设计方向
助手：[读取 Step1_放入资料/示例Project/ 下所有资料]
      → Step 1 需求 & 用户洞察（人话版结论）→ [方向级拍板]
      → Step 2 设计点聚合 + 方案级拍板 → [设计师选择]
      → Step 4 设计策略 → [确认]
      → Step 5 设计规格 → [确认]
      → 输出 DRD 到 Step3_查看分析结果/示例Project/设计需求文档.md（终点）
```

```
用户：只出设计规格，策略我已经定了
助手：[读取已有策略] → 直接执行 Step 5 → 输出设计规格
```
